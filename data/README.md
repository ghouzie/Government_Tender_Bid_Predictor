# Data

`raw/` is reserved for source data or links to source data.

`processed/` should contain the two final modeling tables:

- `tier1_modeling.csv`
- `tier2_modeling.csv`

Large source files should not be committed directly if the repository becomes too large.
Document the source and provide a download link instead.
